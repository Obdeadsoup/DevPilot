spring:
  flyway:
    enabled: true
  datasource:
    hikari:
      maximum-pool-size: 6

management:
  health:
    redis:
      enabled: false

devpilot:
  notification:
    reminder:
      enabled: false
  github:
    base-url: http://127.0.0.1:1
    delivery-recovery-enabled: false
    reconciliation:
      enabled: false
