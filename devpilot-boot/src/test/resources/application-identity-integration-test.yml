spring:
  flyway:
    enabled: true
  datasource:
    hikari:
      maximum-pool-size: 6

management:
  health:
    redis:
      enabled: false

devpilot:
  identity:
    access-token-ttl: 2h
  github:
    delivery-recovery-enabled: false
